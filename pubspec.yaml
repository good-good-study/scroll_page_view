name: scroll_page_view
description: A new Flutter Scroll PageView package.
version: 1.0.0
homepage: https://github.com/good-good-study

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: